# 이 파일을 .env 로 복사한 뒤 값을 채운다.
#   cp .env.example .env
#
# 비워 두면 서버 없이 mock 으로 돈다.
#
# localhost 를 쓰면 안 된다 — 폰 입장에서 localhost 는 폰 자신이다.
# 맥의 LAN IP 를 쓴다: ipconfig getifaddr en0  (폰과 맥이 같은 Wi-Fi 에 있어야 한다)
# 포트는 루트 .env 의 SERVER_PORT 와 맞춘다.
EXPO_PUBLIC_API_URL=http://192.168.0.10:8082
